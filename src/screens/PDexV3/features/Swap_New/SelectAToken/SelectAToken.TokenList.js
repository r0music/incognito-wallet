import React from 'react';
import { StyleSheet } from 'react-native';

import { useNavigation } from 'react-navigation-hooks';

const styled = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'red',
  },
});

const SelectATokenTokenList = (props) => {
  const navigation = useNavigation();
  return <></>;
};

SelectATokenTokenList.propTypes = {};
SelectATokenTokenList.defaultProps = {};

export default SelectATokenTokenList;
